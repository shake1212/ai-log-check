# 规则引擎UI配置 - 文件修改清单

## 📁 新增文件

### 数据库迁移
1. `back-system/src/main/resources/db/migration/V1.0.5__add_rule_engine_config.sql`
   - 添加规则引擎配置字段到数据库表

### Python脚本
2. `back-system/src/scripts/test_rule_engine_integration.py`
   - 规则引擎集成测试脚本
   - 验证所有组件是否正确集成

### 文档
3. `规则引擎集成状态分析.md`
   - 当前状态分析
   - 问题根源分析

4. `规则引擎集成方案.md`
   - 详细技术方案
   - 实现步骤说明

5. `规则引擎UI配置使用说明.md`
   - 详细使用指南
   - 配置说明
   - 故障排查

6. `快速启动指南.md`
   - 5分钟快速上手
   - 简化的启动步骤

7. `实现总结.md`
   - 技术实现细节
   - 修改清单
   - 数据对比

8. `规则引擎UI配置-README.md`
   - 功能概述
   - 快速开始
   - 最佳实践

9. `实施检查清单.md`
   - 实施前检查
   - 实施步骤
   - 验证清单

10. `文件修改清单.md`
    - 本文档

## 📝 修改文件

### Java后端
1. `back-system/src/main/java/com/security/ailogsystem/entity/LogCollectorConfig.java`
   - **修改内容**: 添加规则引擎配置字段
   - **新增字段**:
     ```java
     private Boolean enableRuleEngine = true;
     private Integer ruleEngineTimeout = 10;
     ```

### 前端TypeScript
2. `ai-log-system/src/services/LogCollectorService.ts`
   - **修改内容**: 更新接口定义和模拟数据
   - **新增字段**:
     ```typescript
     enableRuleEngine?: boolean;
     ruleEngineTimeout?: number;
     ```

### 前端UI
3. `ai-log-system/src/pages/log-collector/index.tsx`
   - **修改内容**: 添加规则引擎配置UI组件
   - **新增组件**:
     - 启用规则引擎分析开关（Switch）
     - 规则引擎超时输入框（InputNumber）
     - 工具提示说明

### Python采集器
4. `back-system/src/scripts/unified_log_collector.py`
   - **修改内容**: 集成规则引擎功能
   - **主要修改**:
     - 导入规则引擎模块
     - 配置类添加规则引擎字段
     - 初始化规则引擎客户端
     - 添加配置加载方法
     - 在事件收集后应用规则分析
     - 添加规则引擎统计信息

### 功能状态文档
5. `ai-log-system/功能状态.md`
   - **修改内容**: 更新功能状态
   - **新增内容**:
     - 日志采集器状态更新
     - 规则引擎检测功能更新
     - 威胁检测功能更新
     - 数据库表说明更新
     - 最新更新章节

## 📊 修改统计

### 代码修改
- **Java文件**: 1个
- **TypeScript文件**: 1个
- **React组件**: 1个
- **Python文件**: 1个
- **SQL脚本**: 1个

### 新增文件
- **Python脚本**: 1个
- **文档**: 9个

### 总计
- **修改文件**: 5个
- **新增文件**: 10个
- **总文件数**: 15个

## 🔍 详细修改内容

### 1. LogCollectorConfig.java

**位置**: `back-system/src/main/java/com/security/ailogsystem/entity/LogCollectorConfig.java`

**修改类型**: 添加字段

**修改前**:
```java
@Column(name = "retention_days", nullable = false)
private Integer retentionDays;

@Column(name = "created_at", nullable = false, updatable = false)
private LocalDateTime createdAt;
```

**修改后**:
```java
@Column(name = "retention_days", nullable = false)
private Integer retentionDays;

@Column(name = "enable_rule_engine", nullable = false)
private Boolean enableRuleEngine = true;

@Column(name = "rule_engine_timeout")
private Integer ruleEngineTimeout = 10;

@Column(name = "created_at", nullable = false, updatable = false)
private LocalDateTime createdAt;
```

### 2. LogCollectorService.ts

**位置**: `ai-log-system/src/services/LogCollectorService.ts`

**修改类型**: 接口扩展

**修改前**:
```typescript
export interface LogCollectorConfig {
  id: string;
  name: string;
  // ... 其他字段 ...
  retentionDays: number;
  lastRun?: string;
  nextRun?: string;
}
```

**修改后**:
```typescript
export interface LogCollectorConfig {
  id: string;
  name: string;
  // ... 其他字段 ...
  retentionDays: number;
  enableRuleEngine?: boolean;
  ruleEngineTimeout?: number;
  lastRun?: string;
  nextRun?: string;
}
```

### 3. index.tsx (日志采集器页面)

**位置**: `ai-log-system/src/pages/log-collector/index.tsx`

**修改类型**: UI组件添加

**添加内容**:
```tsx
<Form.Item
  name="enableRuleEngine"
  label="启用规则引擎分析"
  valuePropName="checked"
  tooltip="启用后将对采集的安全事件进行威胁分析和规则匹配"
>
  <Switch />
</Form.Item>

<Form.Item
  name="ruleEngineTimeout"
  label="规则引擎超时（秒）"
  tooltip="规则引擎分析单个事件的最大等待时间"
  rules={[{ type: 'number', min: 1, max: 60 }]}
>
  <InputNumber min={1} max={60} style={{ width: '100%' }} />
</Form.Item>
```

### 4. unified_log_collector.py

**位置**: `back-system/src/scripts/unified_log_collector.py`

**修改类型**: 功能集成

**主要修改**:

#### 4.1 导入规则引擎
```python
try:
    from rule_engine_integration import RuleEngineClient
    RULE_ENGINE_AVAILABLE = True
except ImportError:
    RULE_ENGINE_AVAILABLE = False
```

#### 4.2 配置类扩展
```python
@dataclass
class CollectorConfig:
    # ... 现有字段 ...
    enable_rule_engine: bool = True
    rule_engine_timeout: int = 10
```

#### 4.3 初始化规则引擎
```python
def __init__(self, config: CollectorConfig = None):
    # ... 现有代码 ...
    
    self.rule_engine_client = None
    if RULE_ENGINE_AVAILABLE and self.config.enable_rule_engine:
        self.rule_engine_client = RuleEngineClient(...)
```

#### 4.4 配置加载
```python
def load_config_from_backend(self) -> bool:
    """从Java后端加载配置"""
    # 调用API
    # 更新配置
    # 重新初始化规则引擎
```

#### 4.5 规则引擎分析
```python
def collect_security_logs(self):
    # ... 收集事件 ...
    
    if self.rule_engine_client:
        for event in events:
            analyzed_event = self.rule_engine_client.analyze_event(event)
```

### 5. 功能状态.md

**位置**: `ai-log-system/功能状态.md`

**修改类型**: 状态更新

**修改内容**:
- 日志采集器状态从 ⚠️ 更新为 ✅
- 规则引擎检测功能说明更新
- 威胁检测功能说明更新
- 数据库表说明更新
- 添加最新更新章节

## 📋 Git提交建议

### 提交1: 数据库迁移
```bash
git add back-system/src/main/resources/db/migration/V1.0.5__add_rule_engine_config.sql
git commit -m "feat: 添加规则引擎配置字段到数据库"
```

### 提交2: Java后端
```bash
git add back-system/src/main/java/com/security/ailogsystem/entity/LogCollectorConfig.java
git commit -m "feat: LogCollectorConfig添加规则引擎配置字段"
```

### 提交3: 前端
```bash
git add ai-log-system/src/services/LogCollectorService.ts
git add ai-log-system/src/pages/log-collector/index.tsx
git commit -m "feat: 前端UI添加规则引擎配置界面"
```

### 提交4: Python采集器
```bash
git add back-system/src/scripts/unified_log_collector.py
git commit -m "feat: Python采集器集成规则引擎"
```

### 提交5: 测试脚本
```bash
git add back-system/src/scripts/test_rule_engine_integration.py
git commit -m "test: 添加规则引擎集成测试脚本"
```

### 提交6: 文档
```bash
git add *.md
git add ai-log-system/功能状态.md
git commit -m "docs: 添加规则引擎UI配置文档"
```

## 🔄 回滚方案

如果需要回滚，按以下顺序操作：

### 1. 停止服务
```bash
# 停止Python采集器
# 停止前端
# 停止Java后端
```

### 2. 回滚数据库
```sql
ALTER TABLE log_collector_configs 
DROP COLUMN IF EXISTS enable_rule_engine,
DROP COLUMN IF EXISTS rule_engine_timeout;
```

### 3. 回滚代码
```bash
git revert <commit-hash>
# 或
git reset --hard <previous-commit>
```

### 4. 重新启动服务
```bash
# 启动Java后端
# 启动前端
# 启动Python采集器
```

## ✅ 验证清单

实施后验证：

- [ ] 所有文件都已正确修改
- [ ] 数据库迁移成功
- [ ] Java后端编译成功
- [ ] 前端编译成功
- [ ] Python采集器启动成功
- [ ] 测试脚本全部通过
- [ ] 功能正常工作
- [ ] 文档已更新

## 📞 联系信息

如有问题，请参考：
- `规则引擎UI配置使用说明.md` - 详细使用指南
- `快速启动指南.md` - 快速上手
- `实施检查清单.md` - 实施步骤

---

**版本**: 1.0.0
**创建日期**: 2026-03-30
**最后更新**: 2026-03-30
